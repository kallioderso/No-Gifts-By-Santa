<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="using:No_Gifts_By_Santa.MVVM.ViewModel"
             x:Class="No_Gifts_By_Santa.MVVM.View.MenuView" >
    
    <Shell.BackButtonBehavior>
        <BackButtonBehavior IsVisible="False" IsEnabled="False"/>
    </Shell.BackButtonBehavior>
    
    <ContentPage.Background>
        <LinearGradientBrush EndPoint="0,1">
            <GradientStop Color="#ffffff" Offset="0.0" />
            <GradientStop Color="#FFFFF4BE" Offset="1.0" />
        </LinearGradientBrush>
    </ContentPage.Background>
    
    <ContentPage.BindingContext>
        <local:MenuViewModel/>
    </ContentPage.BindingContext>
    
    <ContentPage.Content>
        <Grid x:Name="MenuPage">
            <VerticalStackLayout Spacing="12" HorizontalOptions="Center" VerticalOptions="Center">
                <Button Text="Level" WidthRequest="{Binding ButtonWidth}" />
                <Button Text="Credits" WidthRequest="{Binding ButtonWidth}" />
                <Button Text="Settings" WidthRequest="{Binding ButtonWidth}" />
                <Button Text="Tutorial" WidthRequest="{Binding ButtonWidth}" />

            </VerticalStackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>